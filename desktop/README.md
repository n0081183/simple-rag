# SIWZ-RAG Lite — Desktop (Tauri)

Optional native wrapper around the FastAPI + Next.js static export.

## Status

Scaffold and documentation only. Run `cargo tauri init` here when you are ready to produce `.app` / `.msi` / `.deb` bundles.

See [docs/desktop-tauri.md](../docs/desktop-tauri.md) for architecture and sidecar notes.

## Quick check (web parity)

```bash
cd ..
make run
# open http://localhost:8000
make e2e
```
